# Movies Server

## Setup

1) Ensure `.env` has these values:

- `APP_PORT = 3001`
- `CLIENT_URL = "http://localhost:3000"`
- `DB_FILE = "data/movies.sqlite"`

2) Install dependencies:

```bash
npm install
```

3) Run the server:

```bash
npm start
```
